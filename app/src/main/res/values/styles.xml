<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--Material Design theme-->
    <style name="MaterialDesign">
        <item name="smallText">@color/primary_text</item>
        <item name="bigText">@color/secondary_text</item>
        <item name="toolbar">@color/accent</item>
        <item name="primary">@color/primary</item>
        <item name="primary_dark">@color/primary_dark</item>
        <item name="primary_light">@color/primary_light</item>
        <item name="icons">@color/icons</item>
        <item name="divider">@color/divider</item>

    </style>

</resources>
